{
  "name": "loomchain-subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for LoomChain tree and node indexing",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio loomchain-subgraph",
    "create-local": "graph create --node http://localhost:8020/ loomchain/loomchain-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ loomchain/loomchain-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 loomchain/loomchain-subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "dependencies": {}
}